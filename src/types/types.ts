export default interface Form {
  id: string;
  name: string;
  referenceId: string;
  created: string;
  deadline: string;
  modifiedBy: string;
  modificationDate: string;
  status: string;
  result: string;
  requiredAction: string;
}
